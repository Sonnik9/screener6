## GOAL

1. Cтремимся к главной цели скринера -- искать монеты похожие паттерну который представлен на фото (target_pattern_1m.png). Биржа Kucoin Futures.

2. Глобальные признаки паттерна (Штрих-код):
    1. Боковик с широким диапазоном.
    2. Длинные тени по отношению к маленьким телам свечей
    3. Постоянный возврат к среднему цены (вокруг некоей оси)
    4. Цена часто двигается от одного края диапазона церез ось к противоположному
    5. Таймфрейм 1 минута


## CRITICAL

1. Планы на версию v14. Делаем рефакторинг расчетной базы проекта:

   1. Оставляем идею benchmark логики. Приспосабливаем под новую архитектуру.
   2. Оставляем daily_volume фильтр
   3. Оставляем approximation
   4. Оставляем atr

   5. Удаляем: donchian, wicks, narrow_penalty

   6. Строим новый главный индикатор: barcode_pattern:
    "barcode_pattern":
      {
        "enable": true,
        "window": 40,
        "strih_threshold_pct": 1.5,
        "high_matches_pctl": 90.0,
        "low_matches_pctl": 90.0
      }
    Идея выделена красными горизонтальными линиями на картинке idea_pattern. То есть это две горизонтальные линии которые пересекают 90-95% касаний цены за указанный период. Нужно найти и внедрить математическое решение вычисления таких состояний по поиску монет. Еще раз: нужно для верхнего уровня (все что выше средней линии) найти максимально высокое значение цены при котором цена пересекалась максимальное количество раз на указанный перцентиль -- high_matches_pctl (например 90%). То же для нижнего диапазона только в меньшую сторону (low_matches_pctl). Между найденными диапазонами находим процентный отступ, то есть %D = ((high_horizint - low_horizont) / low_horizont) * 100 и нужно чтобы это значение было >= strih_threshold_pct. Если все выполнено, то при прочих других условиях (atr (кстати в процентах должен быть, вроде так и есть, но явно выше чем 15%)) и плюс начальный фильтр объема. если все удастся граматно математически решить и построить пейплайн, то считай что победа в кармане.

## NEW

Все хорошо. Версия стабильная и на 80% выполняет свою задачу. Но нужно вернуть параметр 
    "narrow_penalty": {
      "enable": true,
      "min_range_pct": 0.2, // Штраф за мелкие свечи
      "max_penalty_pct": 20.0 // Если набрался такой процент штрафов то монету скипаем.
    }
    